import React from 'react';
import { Typography, Box } from '@mui/material';

const SubCompanyDPage: React.FC = () => (
  <Box sx={{ color: '#F5F8FA', textAlign: 'center', mt: 8 }}>
    <Typography variant="h3" fontWeight={700}>
      Welcome to Sub-Company D Page
    </Typography>
  </Box>
);

export default SubCompanyDPage; 